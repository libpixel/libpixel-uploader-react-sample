# Sample React app with LibPixel Uploader

This sample project shows the [LibPixel
Uploader](https://github.com/libpixel/libpixel-uploader) being used as part of a
larger form in a React application.

Run with `npm start`.
